import { ReplayView } from '../components/replay/ReplayView';

/**
 * ReplayPage is the route-level component for /replay.
 * It delegates all rendering to the ReplayView component.
 */
export function ReplayPage() {
  return <ReplayView />;
}
